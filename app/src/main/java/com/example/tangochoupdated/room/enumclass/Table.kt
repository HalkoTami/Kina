package com.example.tangochoupdated.room.enumclass

enum class Table(value: Int ) {
    USER(0),
    FILE(1),
    CARD(2),
    MARKER_DATA(3),
    ACTIVITY_DATA(4),
    CHOICE(5)
}
enum class Select(value: Int){
    ALL(0),
    AMOUNT(1),
}